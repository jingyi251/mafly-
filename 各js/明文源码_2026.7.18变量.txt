# Cloudflare Worker 环境变量完整参考手册

> 源码文件：`明文源码_2026.7.18.js`  
> 所有变量均在 Cloudflare Worker 的 **Settings → Variables → Environment Variables** 中配置。

---

## 一、🔐 访问鉴权

| 变量名 | 示例值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `ADMIN_TOKEN` | `MySecretKey123` | ✅ 必填 | **自定义密码**。管理面板的登录密钥，填写后浏览器访问面板时需输入此密码。随意设置一个复杂字符串即可。源码读取位置：`request.headers.get('Cookie')` 与 `env.ADMIN_TOKEN` 对比验证。 |

---

## 二、🌩️ Cloudflare API 集成（DNS 解析 / Worker 调度）

> 这组变量用于面板内的「动态 DNS 解析」和「Worker 调度区域修改」功能。不需要这两个功能可以不填。

| 变量名 | 示例值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `CF_API_TOKEN` | `abc123xyz...` | ⚠️ 按需填 | **来自 Cloudflare 后台**。登录 [dash.cloudflare.com](https://dash.cloudflare.com) → 右上角头像 → **My Profile（我的个人资料）** → **API Tokens（API 令牌）** → **Create Token** → 选择 `Edit Cloudflare Workers` 或 `Edit DNS Zone` 模板生成。DNS 解析功能和流量统计查询均需此 Token。源码中检查位置：`L39`、`L102`、`L621`、`L631`。 |
| `CF_ACCOUNT_ID` | `a1b2c3d4e5...` | ⚠️ 按需填 | **来自 Cloudflare 后台**。登录 Cloudflare → 点击进入你的域名或任意一个 Worker → 右侧侧边栏中的「账户 ID」复制粘贴即可（32位十六进制字符串）。面板「提交 Worker 调度区域修改」按钮需要此变量。源码检查位置：`L621`、`L628`。 |
| `CF_WORKER_NAME` | `my-emby-proxy` | ⚠️ 按需填 | **自己确定**。就是该 Worker 在 Cloudflare 后台的「脚本名称」（Workers & Pages → 该 Worker 的名字）。面板「提交 Worker 调度区域修改」按钮需要此变量。源码检查位置：`L621`、`L628`。 |
| `CF_ZONE_ID` | `7f2c8a1b9e...` | ⚠️ 按需填 | **来自 Cloudflare 后台**。登录 Cloudflare → 点击进入你绑定的**域名**（Zone）→ 右侧「Overview」页面下方的「Zone ID」（32位十六进制字符串）。用于 DNS 解析查询和流量统计 GraphQL 查询。源码检查位置：`L39`、`L56`。 |
| `CF_DOMAIN` | `proxy.example.com` | ⚠️ 按需填 | **自定义**。绑定到这个 Worker 的**自定义域名**（不含 `https://`）。当 `STATS_PANEL_URL` 和 `PANEL_URL` 均未设置时，代码会自动用它拼出面板跳转链接 `https://{CF_DOMAIN}`。DNS 解析管理功能也依赖此变量确定要操作的域名。源码位置：`L144`、`L1428`。 |

---

## 三、🛠️ EMOS 官方要求变量

> 参照 EMOS 官方 Wiki 要求，反代 EMOS 节点时必须添加以下变量。

| 变量名 | 推荐值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `EMOS_FORCE_REAL_IP` | `1` | ✅ 必填 | **固定值**。填 `1` 开启，填 `0` 关闭。开启后，Worker 会强制将访客真实 IP 注入 `X-Real-IP` / `X-Forwarded-For` / `X-FORWARDED-FOR` 三个请求头再发给 EMOS 服务器（EMOS 官方要求必须传递）。源码函数：`applyEmosHeaders()`，`L509`。 |
| `EMOS_IMAGE_CACHE_TTL` | `86400` | ✅ 必填 | **固定值**。单位：秒。填 `86400`（24小时）。对 EMOS 节点的图片接口（`/Images/`、`/items/*/images/`）启用 Cloudflare 边缘缓存，减少回源压力。源码：`fetchInit.cf = { cacheEverything: true, cacheTtl: env.EMOS_IMAGE_CACHE_TTL }`，`L1793`。 |
| `EMOS_MATCH_HOSTS` | `emos.best` | ✅ 必填 | **固定值**。逗号分隔的域名列表（如 `emos.best,cdn.emos.best`）。Worker 用此列表识别哪些目标域名属于 EMOS 节点，只有匹配的请求才会应用 EMOS 专属优化逻辑。源码函数：`isEmosTarget()`，`L493`。 |
| `EMOS_PING_CACHE_TTL` | `60` | ✅ 必填 | **固定值**。单位：秒。填 `60`。对 EMOS 的 Ping/健康检测接口（`/system/ping`）单独设置更短的缓存时间，降低无效心跳对服务器的压力。源码：`L1791`。 |
| `EMOS_PROGRESS_THROTTLE_MS` | `1200` | ✅ 必填 | **固定值**。单位：毫秒。填 `1200`。**核心防封控变量**。对播放进度上报接口 `/sessions/playing/progress` 进行节流：同一用户（IP + 设备ID + SessionID 组合）两次上报间隔小于此值时，Worker 直接拦截并返回 `204 No Content`，不回源。源码函数：`shouldThrottleEmosProgress()`，`L581`。 |
| `EMOS_PROXY_ID` | `e123456789` | ✅ 必填 | **由 EMOS 服主/官方分配**。通常在加入 EMOS 服时由服主通过面板或 TG 群提供，格式一般为 `e` 开头的字符串。Worker 将其注入请求头 `EMOS-PROXY-ID` 发往上游，用于 EMOS 官方审计溯源。源码函数：`applyEmosHeaders()`，`L504`。 |
| `EMOS_PROXY_NAME` | `@MyChannel` | ✅ 必填 | **由 EMOS 服主/官方分配**。你的代理频道/名称标识，通常就是你的 TG 频道 Username（含 `@`）。Worker 将其注入请求头 `EMOS-PROXY-NAME` 发往上游。源码函数：`applyEmosHeaders()`，`L505`。 |

---

## 四、📊 面板与 Telegram 推送变量

| 变量名 | 示例值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `STATS_PANEL_URL` | `https://proxy.example.com` | ⚠️ 推荐填 | **自定义**。填写绑定到该 Worker 的完整访问域名（含 `https://`）。用于 TG 定时统计推送消息中的「测速面板」跳转按钮链接。优先级高于 `PANEL_URL` 和 `CF_DOMAIN`。源码位置：`L144`（`tgPanelUrl()` 函数）。 |
| `PANEL_URL` | `https://proxy.example.com` | ⚠️ 备用 | **自定义**。功能与 `STATS_PANEL_URL` 相同，是其旧版别名。若两者都填，优先用 `STATS_PANEL_URL`。源码位置：`L144`。 |
| `TG_BOT_TOKEN` | `7993011408:AAFDKge...` | ✅ 必填 | **来自 Telegram `@BotFather`**。在 TG 搜索 `@BotFather` → 发送 `/newbot` → 按提示填写名称 → 最终获得格式为 `数字:字母串` 的 Token。用于所有 TG 消息推送和 Webhook 接收。源码：`L704`、TG 推送函数中大量使用。 |
| `TG_API_BASE` | `https://api.telegram.org` | ⚠️ 按需填 | **固定值或自建反代地址**。Telegram Bot API 的请求基础地址。国内或无法直连 TG 的环境，填写自建的 TG API 反代域名（如 `https://tg.yourproxy.com`）；可直连的环境直接填默认值即可。源码函数：`tgApiBase(env)`，`L147`。 |
| `TG_CHAT_ID` | `5153827615` | ✅ 必填 | **来自 Telegram**。接收统计推送的**首选**账号 ID 或群组 ID。个人 ID：向 `@userinfobot` 发一条任意消息，它会回复你的数字 ID。群组 ID：将 Bot 加入群组后，向 `@getmyid_bot` 发消息，或通过 API 获取（群组 ID 通常以 `-100` 开头）。源码：`L604`。 |
| `TG_THREAD_ID` | `635` | 🔘 可选 | **来自 Telegram**。如果 `TG_CHAT_ID` 对应的是一个**超级群组（Supergroup）且开启了 Topics（话题）功能**，填写要推送到的话题 ID。话题 ID 可在群内右键话题标题 → 复制链接 → 链接末尾的数字即是。不填则默认推到群主频道。源码：`L441`。 |
| `TG_CHAT_ID_2` | `-1001680439011` | 🔘 可选 | **来自 Telegram**。接收统计推送的**第二个**账号或群组 ID，获取方式同 `TG_CHAT_ID`。常用于同时推到个人账号和用户观影群。源码：`L605`。 |
| `TG_THREAD_ID_2` | `888` | 🔘 可选 | **来自 Telegram**。第二个推送目标群组的话题 ID，用法同 `TG_THREAD_ID`。源码：`L443`。 |
| `AUTHOR_CONTACT_URL` | `https://t.me/sddzn_bot` | 🔘 可选 | **自定义**。填写作者的联系方式链接，默认为 `https://t.me/sddzn_bot`。该变量用于未登录主页的“联系作者获取源码”按钮。源码位置：主页渲染逻辑。 |

---

## 五、🗄️ D1 数据库绑定

| 绑定名 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :---: | :--- |
| `DB` | D1 Database | ✅ 必填 | **在 Cloudflare Worker 的 Settings → Bindings → D1 Database Bindings 中绑定**，绑定变量名必须为 `DB`。这不是环境变量，是数据库绑定。Worker 用它存储反代节点路由（`routes` 表）、TG 会话状态（`tg_sessions` 表）、访客日志（`visitor_logs`、`request_stats` 表）等所有持久化数据。未绑定则面板和 TG Bot 均无法工作。源码：全文大量使用 `env.DB`。 |

---

## 六、📉 数据库容量限制

| 变量名 | 默认值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `DB_LIMIT_ROWS` | `50000` | 🔘 可选 | **自定义数值**。访客日志表（`visitor_logs`）的最大保留行数，超过后自动清理最旧记录，防止 D1 数据库撑满。默认值 `50000` 对免费套餐够用。源码：`const DB_LIMIT = Number(env.DB_LIMIT_ROWS \|\| 50000)`，`L253`。 |

---

## 七、🌍 Worker 出口节点手动标注（可选）

> 当 Worker 自动探测出口节点失败时，会读取以下两个变量作为兜底显示。

| 变量名 | 示例值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `EGRESS_NODE` | `104.21.88.1` | 🔘 可选 | **自定义**。手动填入 Worker 的出口 IP 地址。代码优先通过请求 `https://1.1.1.1/cdn-cgi/trace` 自动获取；若请求失败，才会读取此变量显示在 TG 推送和面板中。源码：`L188`、`L190`。 |
| `EGRESS_LOCATION` | `Tokyo, JP` | 🔘 可选 | **自定义**。手动填入 Worker 出口节点的地理位置描述（格式随意，如 `东京机房`）。与 `EGRESS_NODE` 配套使用，作为自动探测失败时的兜底显示。源码：`L189`、`L190`。 |
| `SERVER_REGION` | `NRT` | 🔘 可选 | **自定义**。Worker 机房的 IATA 代码（如 `NRT` = 东京成田）。代码优先通过 `cf.colo` 自动获取；若获取失败，依次读取 `SERVER_REGION` 或 `WORKER_REGION` 作为兜底。源码：`L186`。 |
| `WORKER_REGION` | `NRT` | 🔘 可选 | **自定义**。`SERVER_REGION` 的旧版别名，用法完全相同，兜底优先级低于 `SERVER_REGION`。源码：`L186`。 |

---

## 八、🕵️ 安全增强（可选）

| 变量名 | 示例值 | 是否必填 | 获取来源 / 说明 |
| :--- | :--- | :---: | :--- |
| `TRUST_FALLBACK_REAL_IP` | `1` | 🔘 可选 | **固定值**。填 `1` 开启，默认 `0`（关闭）。**开启后 Worker 会信任并透传 `X-Forwarded-For` / `X-Real-IP` 请求头**中的 IP（这两个头客户端可以伪造）。**仅在你的 Worker 前面还有一层可信反代（如 Nginx）时才建议开启**，普通部署请保持默认关闭，否则有 IP 伪造风险。源码函数：`getRealClientIp()`，`L473`。 |

---

## 配置优先级速查

```text
面板跳转链接：STATS_PANEL_URL > PANEL_URL > CF_DOMAIN（自动拼接 https://）
Worker 机房显示：cf.colo（自动） > SERVER_REGION > WORKER_REGION
出口 IP 显示：1.1.1.1/trace（自动） > EGRESS_NODE + EGRESS_LOCATION
```

---

## 九、🔑 CF_API_TOKEN 权限配置指南

> 本节记录 `CF_API_TOKEN` 在 Cloudflare 后台需要开通的所有权限，确保面板所有功能正常运作。

### 创建步骤

1. 登录 [dash.cloudflare.com](https://dash.cloudflare.com)
2. 右上角头像 → **My Profile** → 左侧 **API Tokens** → **Create Token**
3. 选底部的 **Create Custom Token（自定义 Token）**
4. 按下表添加 5 个权限条目
5. Account Resources 选择你的账号，Zone Resources 选 `Specific zone` → 你绑定的域名
6. 点击 **Continue to summary → Create Token** → 复制并保存

### 所需权限清单（经源码核实，共 4 条）

| # | 第一列（类型） | 第二列（权限名） | 第三列（等级） | 对应功能 |
|---|---|---|---|---|
| 1 | **帐户** | **Workers 脚本** | **编辑** | Worker 代码读取/更新/覆盖部署、读取绑定信息、修改 Smart Placement 调度设置 |
| 2 | **区域** | **清除缓存** | **清除** | 面板"一键清除 CDN 缓存"按钮 |
| 3 | **区域** | **DNS** | **编辑** | 读取/写入/删除 DNS 解析记录（优选 IP 后更新 DNS 功能）|
| 4 | **区域** | **Analytics** | **读取** | 流量统计图表（GraphQL 查询区域流量数据）|

> ⚠️ 原来给出的第 5 条 `Workers 路由` 是 `wrangler deploy` 部署时的权限，与面板运行时的 `CF_API_TOKEN` 无关，不需要添加。
> 你截图中多余的 `DNS 设置` 和 `区域设置` 也可以删掉。



### 特别说明

- 若只使用反代转发，不需要任何 CF 高级功能，`CF_API_TOKEN` 可以留空，所有依赖它的面板按钮会返回"变量未配置"的友好提示，不影响反代核心功能。
- 旧版全局 API Key（Global API Key，即纯 32 位十六进制字符串）**无法作为 Bearer Token 使用**，必须在后台生成专用 API Token。
- Token 创建后请妥善保管，Cloudflare 只显示一次，丢失后只能删除重建。

---

## 十、🤖 Telegram Bot Webhook 绑定与触发说明

> 本 Worker 内置完整的 Telegram 机器人交互后端，支持在 TG 内直接管理反代节点、查看系统状态及处理异步菜单。

### 1. Webhook 接口地址
你的 Worker 提供的 Telegram Webhook 接收端点为：
```text
https://<你的Worker域名或自定义域名>/api/tg-webhook
```

### 2. 绑定 Webhook 触发方式

在开启 TG Bot 交互功能前，需要将该接口注册到 Telegram 官方服务器。

#### 方式一：浏览器直接访问注册（最简便）
将下方链接中的 `<TG_BOT_TOKEN>` 替换为你的机器人 Token，`<YOUR_DOMAIN>` 替换为你的域名，然后在浏览器中打开：
```text
https://api.telegram.org/bot<TG_BOT_TOKEN>/setWebhook?url=https://<YOUR_DOMAIN>/api/tg-webhook
```
**成功提示**：浏览器会返回 `{"ok":true,"result":true,"description":"Webhook was set"}`。

#### 方式二：使用 cURL / PowerShell 命令行注册
在终端中运行以下命令：
```bash
curl -s "https://api.telegram.org/bot<TG_BOT_TOKEN>/setWebhook?url=https://<YOUR_DOMAIN>/api/tg-webhook"
```

---

### 3. 检查与测试 Webhook 状态

#### 检查 Worker 端健康状态
直接在浏览器中访问：`https://<YOUR_DOMAIN>/api/tg-webhook` (GET 请求)  
返回示例：
```json
{
  "ok": true,
  "endpoint": "/api/tg-webhook",
  "has_db": true,
  "has_tg_token": true,
  "tg_api_base": "https://api.telegram.org"
}
```

#### 检查 Telegram 官方回调状态
在浏览器访问：
```text
https://api.telegram.org/bot<TG_BOT_TOKEN>/getWebhookInfo
```

---

### 4. 解除 Webhook 绑定
若需要停用机器人 Webhook，在浏览器中访问：
```text
https://api.telegram.org/bot<TG_BOT_TOKEN>/deleteWebhook
```

---

### 5. TG Bot 支持的命令与触发逻辑
- `/status` 或 点击菜单按钮【📊 查看运行状态】：获取节点运行状态与实时统计图表
- 点击菜单按钮【🌐 获取反代节点】：向机器人获取当前已部署的反代节点列表及跳转链接
- `/cancel` 或 发送 `取消`：随时中断当前的节点编辑/添加会话
